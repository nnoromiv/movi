/* The code is declaring a module named '@env' which exports three constants: BASE_URL, API_KEY, and
BASE_IMG_URL. These constants are of type string and can be used to store environment variables or
configuration values in a TypeScript project. */
declare module '@env' {
    export const BASE_URL: string;
    export const API_KEY: string;
    export const BASE_IMG_URL: string;
}